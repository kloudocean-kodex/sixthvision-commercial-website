# Cloudflare Pages headers.
#
# Caching strategy: the HTML must always revalidate (so a deploy is visible
# immediately), while /assets/* is immutable and cached for a year. That is only
# safe because every CSS/JS reference carries a ?v= query — bump it when you
# swap a same-named asset, or viewers keep the old one for a year.

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: geolocation=(), microphone=(), camera=(), interest-cohort=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://www.google-analytics.com https://www.googletagmanager.com; font-src 'self'; connect-src 'self' https://www.google-analytics.com https://analytics.google.com https://api.web3forms.com https://*.google-analytics.com https://cloudflareinsights.com; media-src 'self'; frame-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self' https://api.web3forms.com

/
  Cache-Control: public, max-age=0, must-revalidate

/index.html
  Cache-Control: public, max-age=0, must-revalidate

/404.html
  Cache-Control: public, max-age=0, must-revalidate

# Extensionless ranking and proof routes are HTML too; force edge revalidation
# so a new Pages deployment cannot leave stale route responses behind.
/commercial-property-photography-melbourne/*
  Cache-Control: public, max-age=0, must-revalidate

/work/*
  Cache-Control: public, max-age=0, must-revalidate

# Extensionless HTML ranking/support routes: always revalidate at the edge.
/construction-photography-melbourne/*
  Cache-Control: public, max-age=0, must-revalidate

/precision-floor-plans/*
  Cache-Control: public, max-age=0, must-revalidate

/sitemap.xml
  Cache-Control: public, max-age=3600

/robots.txt
  Cache-Control: public, max-age=3600

# Fingerprinted, content-addressed media — safe to hold for a year.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/assets/fonts/*
  Cache-Control: public, max-age=31536000, immutable
  Access-Control-Allow-Origin: *
